[package]
name = "sub-frontend"
version = "2.0.0"
edition = "2021"

[dependencies]
rayon = "1.8"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

[lib]
name = "sub_frontend"
crate-type = ["staticlib", "cdylib"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
