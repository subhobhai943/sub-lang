# Test: Nested if/else
# Tests elsif chain handling in Ruby

#embed ruby
    x = 15

    if x > 20
      puts "Greater than 20"
    elsif x > 10
      puts "Greater than 10"
    elsif x > 5
      puts "Greater than 5"
    else
      puts "5 or less"
    end

    # Nested if inside if
    if x > 0
      if x > 10
        puts "Positive and greater than 10"
      else
        puts "Positive but 10 or less"
      end
    end
#endembed
